# 步态奖励与调参

2026-09-14 更新。目标是左右交替步行。先看是否有真实抬脚和持续单脚支撑，再看总回报。

## 当前规则

- **接触**：完整的左右接触组合符合目标、且支撑和摆动持续至少 40 ms，才得正分。短暂匹配得 0，错误组合得 -1；双脚同时无支撑属于错误组合。
- **过渡**：一个周期的 20% 用于双支撑，避免要求左右脚瞬间交换。
- **抬脚**：目标沿摆动相位从 0 平滑升至峰值，再降回 0。只有摆动脚离开接触、对侧持续支撑时才奖励；几毫米的卸载只能获得很少收益。
- **落脚**：无接触超过 120 ms、对侧在整个摆动期间保持支撑、且左右交替落脚，才支付一次奖励。双脚同一策略步内落脚不支付。
- **腾空**：每个 5 ms 物理步检查双脚接触；连续无支撑超过 10 ms 后计罚，即使策略步结束前已经落地也保留惩罚。
- **身体高度**：参考骨盆下方地面；抬起一只脚不会改变高度基准。每只脚的离地高度也分别参考脚下地形。

接触力短暂丢失不足 10 ms 时保留支撑状态，用于步态和落脚判定；腾空惩罚与支撑比例仍使用未滤波的物理接触。脚高度用 body 原点减去脚底偏移估算，未补偿脚倾斜。

接触、抬脚和落脚奖励只作用于平移命令大于 0.1 m/s 的情况；站立命令不强制交替。腾空惩罚对站立和移动均生效。

## 配置

四套环境 YAML 的 `gait:` 和 `rewards:` 配置使用相同规则。完整配置在 [config/presets](source/purerl/purerl/config/presets/)。

| 参数 | Flat | Rough |
|---|---:|---:|
| 完整左右周期 `cycle_time` | 0.5 s | 0.5 s |
| 双支撑比例 `double_support_fraction` | 0.2 | 0.2 |
| 抬脚峰值 `target_feet_height` | 0.04 m | 0.06 m |
| 抬脚计分尺度 `min_clearance` | 0.02 m | 0.02 m |
| 接触奖励权重 | 1.0 | 1.0 |
| 抬脚奖励权重 | 0.5 | 0.5 |
| 落脚事件权重 | 0.75 | 0.5 |
| 持续无支撑惩罚权重 | -1.0 | -1.0 |

`commands.lin_vel_tracking_std=0.35` 控制速度误差容差，越小要求越严格。落脚奖励按事件支付，其余奖励按时间积分。观测仍为 261 维。

## 如何判断效果

训练日志新增 `Gait/` 指标：单脚支撑比例、双脚无支撑比例、相位符合率、摆动脚平均高度、有效落脚频率/比例和速度误差。这些指标按每个回合实际采集的移动命令时间归一化；全程站立的回合记为 0。

某轮没有回合结束时，不写入回合指标，避免空数组平均产生 NaN。

用固定命令测量 checkpoint：

```bash
python scripts/tools/measure_gait.py \
  --checkpoint /path/to/run/model_1000.pt \
  --num-envs 8 --steps 1200 --vx 0.5 \
  --output reports/gait_evaluation
```

输出包括 JSON 指标、轨迹和实际评估配置；使用当前 Flat-Play 配置，模型目录需保留 `params/agent.yaml`。

建议先确认交替落脚，再提高抬脚目标或速度范围。仅修改奖励后直接 Play 旧模型不会改变动作。261 维模型可以用 `--pretrained-checkpoint` 开始新实验，但旧弹跳策略可能难以摆脱；正式基线建议从头训练。

诊断依据：[旧模型回放](reports/flat_baseline_20260914/analysis.md)。修复和验证记录：[TRAINING_FIXES.md](TRAINING_FIXES.md)。
